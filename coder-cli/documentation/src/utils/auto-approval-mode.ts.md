# src/utils/auto-approval-mode.ts

## Summary
Defines the `AutoApprovalMode` enum for controlling command approval policies (suggest, auto-edit, full-auto) and `FullAutoErrorMode` for handling errors under full-auto mode.